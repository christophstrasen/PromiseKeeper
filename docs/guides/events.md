# Guide: situations from events (PZ Events and LuaEvent)

TODO:
- `pk.situations.defineFromPZEvent(...)`
- `pk.situations.defineFromLuaEvent(...)`
- mapper signature: `mapEventToCandidate(situationArgs, ...)`

