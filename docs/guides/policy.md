# Guide: policy (deterministic semantics)

TODO:
- `maxRuns` (per promiseId)
- `chance` (deterministic per occurranceKey, no re-roll)
- `cooldownSeconds` (per promiseId)
- `retry` (per occurranceKey + pacemaker)
- `expiry` safety valve (only when many unfulfilled occurrences)

