# Guide: lifecycle (promise / remember / stop / forget)

TODO:
- `pk.promise(...)` starts the promise immediately.
- `pk.remember()` resumes persisted promises after reload.
- `promise.stop()` vs `promise.forget()` (and why smokes typically forget for iteration).

