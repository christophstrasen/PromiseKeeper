# Guide: WorldObserver integration

TODO:
- WO is the sensing layer, PK is the acting/persistence layer.
- One-time bridge: `pk.situations.searchIn(WorldObserver)`.
- Convention: same namespace for WO + PK.
- Interest leases remain a WO concern (explicit).

