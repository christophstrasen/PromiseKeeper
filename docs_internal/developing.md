# PromiseKeeper — Developing (internal notes)

This file is internal notes for contributors. For the canonical local dev commands (tests, sync, watch), see `../development.md`.

## Pre-commit (optional)

If you use `pre-commit`, install it and enable hooks:

```bash
pre-commit install
pre-commit run --all-files
```
